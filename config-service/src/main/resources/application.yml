server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hugovallada/curso-spring-cloud-config-server
          search-paths:
            - 'greeting-service*'